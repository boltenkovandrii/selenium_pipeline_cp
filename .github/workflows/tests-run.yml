name: Run tests with Selenium Grid

on:
  workflow_dispatch:
    inputs:
      browser:
        description: 'Browser to use (chrome, firefox, edge)'
        required: true
        default: 'firefox'
      threads:
        description: 'Number of parallel browser nodes to run'
        required: true
        default: '2'
      tags:
        description: 'Test tag to include (overrides default)'
        required: true
        default: 'regression'
      runner-image-tag:
        description: 'Tag of runner image to pull from ghcr'
        required: true
        default: 'latest'
      hub-image-tag:
        description: 'Tag of hub image to pull from ghcr'
        required: true
        default: 'latest'
      browser-image-tag:
        description: 'Tag of browser images (chrome, firefox, edge) to pull from ghcr'
        required: true
        default: 'latest'


jobs:

  run-tests:
    name: Bring up grid, run Gradle tests, collect reports

    runs-on: ubuntu-latest
    # need permission to upload report
    permissions:
      contents: write
      packages: read

    env:
      BROWSER: ${{ github.event.inputs.browser || 'firefox' }}
      THREADS: ${{ github.event.inputs.threads || 2 }}
      TAGS: ${{ github.event.inputs.tags || 'regression' }}
      RUNNER_IMAGE_TAG: ${{ github.event.inputs['runner-image-tag'] || 'latest' }}
      HUB_IMAGE_TAG: ${{ github.event.inputs['hub-image-tag'] || 'latest' }}
      BROWSER_IMAGE_TAG: ${{ github.event.inputs['browser-image-tag'] || 'latest' }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to ghcr
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Docker Compose
        run: |
          sudo apt-get update -y
          sudo apt-get install -y docker-compose

      - name: Prepare Downloads dir
        run: |
          mkdir -p Downloads
          chmod a+w Downloads

      - name: Start Selenium Grid
        env:
          REPO_OWNER: ${{ github.repository_owner }}
          REPO_NAME: ${{ github.event.repository.name }}
        run: |
          docker network create selenium-grid-network || true
          docker-compose -f docker-compose-gh.yml up --scale selenium-${{ env.BROWSER }}=${{ env.THREADS }} -d

      - name: Show running containers
        run: |
          docker ps --format "{{.Names}} {{.Image}}"

      - name: Run Gradle tests
        run: |
          chmod +x gradlew
          ./gradlew -DuseGrid=true -Dbrowser=$BROWSER -Dthreads=$THREADS -DincludeTags=$TAGS -Ddownloads=Downloads clean test allureReport || true

      - name: Collect container logs
        run: |
          mkdir -p logs
          docker ps -a --format "{{.Names}}" | while read c; do docker logs "$c" > "logs/$c.log" || true; done || true

      - name: Tear down
        run: |
          docker-compose down || true

      - name: Deploy Allure report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: build/reports/allure-report/allureReport
          destination_dir: reports/${{ github.ref_name }}/${{ github.run_id }}

      - name: Prepare public artifacts
        run: |
          mkdir -p public/Downloads
          mkdir -p public/allureReport
          if [ -d build/reports/allure-report/allureReport ]; then cp -r build/reports/allure-report/allureReport/* public/allureReport/ || true; fi
          if [ -d Downloads ]; then cp -r Downloads/* public/Downloads/ || true; fi
          tar -czf public-archives.tar.gz public || true

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: public-artifacts
          path: public-archives.tar.gz
          retention-days: 3

      - name: Upload logs
        uses: actions/upload-artifact@v4
        with:
          name: container-logs
          path: logs
          retention-days: 3

      - name: Publish artifacts
        run: |
          REPORT_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/reports/${{ github.ref_name }}/${{ github.run_id }}/"
          
          echo "## 🧪 Allure Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "[Open Allure Report]($REPORT_URL)" >> $GITHUB_STEP_SUMMARY